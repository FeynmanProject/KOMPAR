# ---------- Node / Frontend ----------
node_modules/
dist/
.vite/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Root-level npm wart (project's real deps live in
# anime-parallel-recommender/frontend/package.json — these were accidentally
# installed at workspace root by running `npm install` in the wrong cwd).
/package.json
/package-lock.json

# ---------- Python / Backend ----------
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/
.python-version

# Generated SQLite from preprocessing.py (regenerate locally with
# `python preprocessing.py --target-size 10000`)
anime-parallel-recommender/backend/anime.db
anime-parallel-recommender/backend/anime.db-journal

# ---------- Env / Secrets ----------
.env
.env.*
!.env.example

# ---------- OS / IDE ----------
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~
